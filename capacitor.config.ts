import type { CapacitorConfig } from "@capacitor/cli";

// Bundle ID and app name are placeholders — update before store submission.
const config: CapacitorConfig = {
  appId: "com.example.dishplacer",
  appName: "Dish Placer",
  webDir: "out",
};

export default config;
